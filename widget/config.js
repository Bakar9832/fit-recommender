// Frontend config for the Fit Recommender demo pages (storefront, demo, widget).
// Point this at your deployed API origin. Loaded (as a classic script) before the
// page scripts, so it's the single place to change the API URL.
//
// Local dev: set this to "http://localhost:3000" (or clear it to fall back to the
// page's own origin). Production/demo: the deployed Render API URL below.
window.FITW_API_BASE = "https://fit-recommender.onrender.com";
